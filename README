Installation:

    git clone git://github.com/yuanweicn/vimsetup.git ~/.vim

Create symlinks:
    mv .vimrc ~/.vim/vimrc
    ln -s ~/.vim/vimrc ~/.vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
